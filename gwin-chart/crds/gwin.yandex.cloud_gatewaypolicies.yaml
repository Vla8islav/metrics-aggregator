---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: gatewaypolicies.gwin.yandex.cloud
spec:
  group: gwin.yandex.cloud
  names:
    kind: GatewayPolicy
    listKind: GatewayPolicyList
    plural: gatewaypolicies
    singular: gatewaypolicy
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: "The policy is restricted to resources within the same namespace.
              \n When determining which resources the policy applies to, the following
              rules are used: - If both TargetRefs and Selector are specified, a resource
              must match both criteria to be affected by the policy. - If neither
              TargetRefs nor Selector is specified, the policy applies to all resources
              within the same namespace."
            properties:
              policy:
                description: Cloud specific configuration.
                properties:
                  allowZonalShift:
                    description: Specifies whether application load balancer is available
                      to zonal shift.
                    type: boolean
                  autoScale:
                    description: "Scaling settings of the application load balancer.
                      \n The scaling settings relate to a special internal instance
                      group which facilitates the balancer's work. Instances in this
                      group are called _resource units_. The group is scaled automatically
                      based on incoming load and within limitations specified in these
                      settings. \n For details about the concept, see [documentation](/docs/application-load-balancer/concepts/application-load-balancer#lcu-scaling)."
                    properties:
                      maxSize:
                        description: "Upper limit for the total number of resource
                          units across all availability zones. \n If a positive value
                          is specified, it must be at least [min_zone_size] multiplied
                          by the size of [AllocationPolicy.locations]. \n If the value
                          is 0, there is no upper limit."
                        format: int64
                        type: integer
                      minZoneSize:
                        description: "Lower limit for the number of resource units
                          in each availability zone. \n If not specified previously
                          (using other instruments such as management console), the
                          default value is 2. To revert to it, specify it explicitly.
                          \n The minimum value is 2."
                        format: int64
                        type: integer
                    type: object
                  listener:
                    additionalProperties:
                      properties:
                        http:
                          description: HTTP specific listener settings.
                          properties:
                            protocolSettings:
                              properties:
                                allowHTTP10:
                                  description: Enables support for incoming HTTP/1.0
                                    and HTTP/1.1 requests and disables it for HTTP/2
                                    requests.
                                  type: boolean
                                http2Options:
                                  description: HTTP/2 settings. If specified, incoming
                                    HTTP/2 requests are supported by the listener.
                                  properties:
                                    maxConcurrentStreams:
                                      description: Maximum number of concurrent HTTP/2
                                        streams in a connection.
                                      format: int64
                                      type: integer
                                  type: object
                              type: object
                          type: object
                        rbac:
                          properties:
                            action:
                              description: The action to take if a principal matches.
                                Every action either allows or denies a request.
                              enum:
                              - ALLOW
                              - DENY
                              type: string
                            principals:
                              additionalProperties:
                                additionalProperties:
                                  properties:
                                    any:
                                      description: Match any request.
                                      type: boolean
                                    header:
                                      description: A header (or pseudo-header such
                                        as :path or :method) of the incoming HTTP
                                        request.
                                      properties:
                                        exact:
                                          type: string
                                        name:
                                          description: The name of the header to match.
                                          type: string
                                        prefix:
                                          type: string
                                        regex:
                                          type: string
                                      type: object
                                    ip:
                                      description: A CIDR block or IP that describes
                                        the request remote/origin address, e.g. “192.0.0.0/24“
                                        or“192.0.0.4“ .
                                      properties:
                                        remoteIp:
                                          type: string
                                      type: object
                                  type: object
                                type: object
                              description: A match occurs when at least one matches
                                the request.
                              type: object
                          type: object
                        securityProfileID:
                          description: Security profile that will take effect to all
                            requests routed via particular route.
                          type: string
                        stream:
                          description: Stream specific listener settings.
                          properties:
                            idleTimeout:
                              description: The idle timeout is duration during which
                                no data is transmitted or received on either the upstream
                                or downstream connection.
                              type: string
                          type: object
                      type: object
                    type: object
                  listeners:
                    properties:
                      http:
                        description: HTTP specific listener settings.
                        properties:
                          protocolSettings:
                            properties:
                              allowHTTP10:
                                description: Enables support for incoming HTTP/1.0
                                  and HTTP/1.1 requests and disables it for HTTP/2
                                  requests.
                                type: boolean
                              http2Options:
                                description: HTTP/2 settings. If specified, incoming
                                  HTTP/2 requests are supported by the listener.
                                properties:
                                  maxConcurrentStreams:
                                    description: Maximum number of concurrent HTTP/2
                                      streams in a connection.
                                    format: int64
                                    type: integer
                                type: object
                            type: object
                        type: object
                      rbac:
                        properties:
                          action:
                            description: The action to take if a principal matches.
                              Every action either allows or denies a request.
                            enum:
                            - ALLOW
                            - DENY
                            type: string
                          principals:
                            additionalProperties:
                              additionalProperties:
                                properties:
                                  any:
                                    description: Match any request.
                                    type: boolean
                                  header:
                                    description: A header (or pseudo-header such as
                                      :path or :method) of the incoming HTTP request.
                                    properties:
                                      exact:
                                        type: string
                                      name:
                                        description: The name of the header to match.
                                        type: string
                                      prefix:
                                        type: string
                                      regex:
                                        type: string
                                    type: object
                                  ip:
                                    description: A CIDR block or IP that describes
                                      the request remote/origin address, e.g. “192.0.0.0/24“
                                      or“192.0.0.4“ .
                                    properties:
                                      remoteIp:
                                        type: string
                                    type: object
                                type: object
                              type: object
                            description: A match occurs when at least one matches
                              the request.
                            type: object
                        type: object
                      securityProfileID:
                        description: Security profile that will take effect to all
                          requests routed via particular route.
                        type: string
                      stream:
                        description: Stream specific listener settings.
                        properties:
                          idleTimeout:
                            description: The idle timeout is duration during which
                              no data is transmitted or received on either the upstream
                              or downstream connection.
                            type: string
                        type: object
                    type: object
                  logs:
                    description: Cloud logging settings of the application load balancer.
                    properties:
                      disable:
                        description: Do not send logs to Cloud Logging log group.
                        type: boolean
                      discardRule:
                        additionalProperties:
                          properties:
                            discardPercent:
                              description: 'Percent of logs to be discarded: 0 - keep
                                all, 100 or unset - discard all'
                              format: int64
                              type: integer
                            grpcCodes:
                              description: GRPC codes that should be discarded
                              items:
                                type: string
                              type: array
                            httpCodeIntervals:
                              description: Groups of HTTP codes like 4xx that should
                                be discarded.
                              items:
                                type: string
                              type: array
                            httpCodes:
                              description: HTTP codes that should be discarded.
                              items:
                                format: int64
                                type: integer
                              type: array
                          type: object
                        description: DiscardRule is a map where each key represents
                          a user-defined ID for log discard rules. Key names don't
                          matter.
                        type: object
                      logGroupID:
                        description: Cloud Logging log group ID to store access logs.
                          If not set then logs will be stored in default log group
                          for the folder where load balancer located.
                        type: string
                    type: object
                  securityGroups:
                    description: Security groups of load balancer.
                    items:
                      type: string
                    type: array
                  subnets:
                    description: Subnets of the zones where load balancer will be
                      instantiated.
                    items:
                      type: string
                    type: array
                  zone:
                    additionalProperties:
                      properties:
                        receiveTraffic:
                          description: "Enables the load balancer node in the specified
                            availability zone. \n Backends in the availability zone
                            are not directly affected by this setting. They still
                            may receive traffic from the load balancer nodes in other
                            availability zones, subject to [LoadBalancingConfig.locality_aware_routing_percent]
                            and [LoadBalancingConfig.strict_locality] settings."
                          type: boolean
                      required:
                      - receiveTraffic
                      type: object
                    description: Zone is a map where each key is a zone identifier
                      (e.g., ru-central1-a),
                    type: object
                type: object
              selector:
                description: A label selector is a label query over a set of resources.
                  The result of matchLabels and matchExpressions are ANDed. An empty
                  label selector matches all objects. A null label selector matches
                  no objects.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: A label selector requirement is a selector that
                        contains values, a key, and an operator that relates the key
                        and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: operator represents a key's relationship to
                            a set of values. Valid operators are In, NotIn, Exists
                            and DoesNotExist.
                          type: string
                        values:
                          description: values is an array of string values. If the
                            operator is In or NotIn, the values array must be non-empty.
                            If the operator is Exists or DoesNotExist, the values
                            array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: matchLabels is a map of {key,value} pairs. A single
                      {key,value} in the matchLabels map is equivalent to an element
                      of matchExpressions, whose key field is "key", the operator
                      is "In", and the values array contains only "value". The requirements
                      are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              targetRefs:
                items:
                  description: "LocalObjectReference identifies an API object within
                    the namespace of the referrer. The API object must be valid in
                    the cluster; the Group and Kind must be registered in the cluster
                    for this reference to be valid. \n References to objects with
                    invalid Group and Kind are not valid, and must be rejected by
                    the implementation, with appropriate Conditions set on the containing
                    object."
                  properties:
                    group:
                      description: Group is the group of the referent. For example,
                        "gateway.networking.k8s.io". When unspecified or empty string,
                        core API group is inferred.
                      maxLength: 253
                      pattern: ^$|^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                      type: string
                    kind:
                      description: Kind is kind of the referent. For example "HTTPRoute"
                        or "Service".
                      maxLength: 63
                      minLength: 1
                      pattern: ^[a-zA-Z]([-a-zA-Z0-9]*[a-zA-Z0-9])?$
                      type: string
                    name:
                      description: Name is the name of the referent.
                      maxLength: 253
                      minLength: 1
                      type: string
                  required:
                  - group
                  - kind
                  - name
                  type: object
                type: array
            required:
            - policy
            type: object
          status:
            properties:
              attachedGateways:
                description: Number of currently attached gateways.
                format: int32
                type: integer
              conditions:
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource. --- This struct is intended for direct
                    use as an array at the field path .status.conditions.  For example,
                    \n type FooStatus struct{ // Represents the observations of a
                    foo's current state. // Known .status.conditions.type are: \"Available\",
                    \"Progressing\", and \"Degraded\" // +patchMergeKey=type // +patchStrategy=merge
                    // +listType=map // +listMapKey=type Conditions []metav1.Condition
                    `json:\"conditions,omitempty\" patchStrategy:\"merge\" patchMergeKey:\"type\"
                    protobuf:\"bytes,1,rep,name=conditions\"` \n // other fields }"
                  properties:
                    lastTransitionTime:
                      description: lastTransitionTime is the last time the condition
                        transitioned from one status to another. This should be when
                        the underlying condition changed.  If that is not known, then
                        using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: message is a human readable message indicating
                        details about the transition. This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: observedGeneration represents the .metadata.generation
                        that the condition was set based upon. For instance, if .metadata.generation
                        is currently 12, but the .status.conditions[x].observedGeneration
                        is 9, the condition is out of date with respect to the current
                        state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: reason contains a programmatic identifier indicating
                        the reason for the condition's last transition. Producers
                        of specific condition types may define expected values and
                        meanings for this field, and whether the values are considered
                        a guaranteed API. The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                        --- Many .condition.type values are consistent across resources
                        like Available, but because arbitrary conditions can be useful
                        (see .node.status.conditions), the ability to deconflict is
                        important. The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
