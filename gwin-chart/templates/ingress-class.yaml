{{ if .Values.ingressClass.create }}

apiVersion: networking.k8s.io/v1
kind: IngressClass
metadata:
  name: {{ .Values.ingressClass.name }}
spec:
  controller: gwin.yandex.cloud/controller

{{ end }}
